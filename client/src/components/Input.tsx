const Input = () => {
  return (
    <input
      type="text"
      placeholder="Search..."
      autoComplete="off"
      className="font-source w-full text-base text-black dark:text-white bg-transparent focus-within:outline-none px-4"
    />
  );
};

export default Input;
