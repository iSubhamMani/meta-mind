const Input = () => {
  return (
    <input
      type="text"
      placeholder="Search..."
      autoComplete="off"
      className="w-full bg-transparent focus-within:outline-none px-4"
    />
  );
};

export default Input;
