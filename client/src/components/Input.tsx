const Input = () => {
  return (
    <input
      type="text"
      placeholder="Search..."
      className="w-full bg-transparent focus-within:outline-none px-4"
    />
  );
};

export default Input;
